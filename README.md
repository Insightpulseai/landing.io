# landing.io

Redirect shell for InsightPulseAI ERP login.

## Owns

- Lightweight redirect behavior
- Minimal landing-shell configuration

## Does not own

- Primary marketing site (`web`)
- ERP runtime (`odoo`)
- Control-plane logic (`platform`)

## Position

Adjunct redirect-shell repository. Candidate to fold into `web/apps/landing-redirect` when web repo is hydrated.

## Related

- `web` — browser-facing surfaces
- `odoo` — ERP runtime

## Status

Adjunct redirect shell (lifecycle: redirect).
